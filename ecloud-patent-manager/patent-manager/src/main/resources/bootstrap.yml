spring:
  application:
    name: sys-case
  cloud:
    config:
      uri: http://192.168.1.115:8888
      fail-fast: true
      password: admin
      username: user

eureka:
  instance:
      statusPageUrlPath: /case/info
      metadata-map:
        management.context-path: /case
