spring:
  application:
    name: e-center
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: admin
      username: user

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    server:
      waitTimeInMsWhenSyncEmpty: 0