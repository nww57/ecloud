spring:
  application:
    name: sys-admin
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: admin
      username: user

eureka:
  instance:
      statusPageUrlPath: /sysamdin/info
      metadata-map:
        management.context-path: /sysamdin