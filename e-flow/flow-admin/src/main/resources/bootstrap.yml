spring:
  application:
    name: sys-flow
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: admin
      username: user

eureka:
  instance:
      statusPageUrlPath: /flow/info
      metadata-map:
        management.context-path: /flow

